<h1><%= @title %></h1>

<% if search_enabled? %>
    <table>
        <% @pagerank.each do |key, value| %>
            <tr><th><%= key %></th><td><%= value %></td></tr>
        <% end %>
    </table>
<% else %>
    <p><p>Performance issues again :) We are sorry.</p></p>
<% end %>
